# Plan: Update DeepResearch IEEE Report

## Goal
Modify `DeepResearch_IEEE_Report.tex` to reflect the evaluation framework and report-generation improvements designed in this session.

## Background
During this session we:
1. Designed a comprehensive **11-dimension evaluation matrix** comparing DeepResearch against a single-pass RAG baseline.
2. Identified **Mermaid diagram syntax errors** in generated reports and designed a post-processing fixer (`mermaid_fixer.py`) to auto-quote unquoted special characters in node labels.
3. Updated the project `.gitignore` (non-paper change).

The IEEE report currently contains experimental tables for the critique loop (Table 1) and accumulator efficiency (Table 2), but it does **not** yet include:
- The end-to-end evaluation matrix.
- Any mention of Mermaid visual elements in generated reports.
- The Mermaid syntax-validation post-processing step.

## Proposed Changes

### 1. Add Comprehensive Evaluation Matrix
- **Location:** Section V (Experimental Evaluation), after the existing tables.
- **Action:** Insert a new subsection (e.g., `\subsection{Comprehensive Evaluation Matrix}`) containing a wide LaTeX table (`table*` or `table` with `\scriptsize`) that compares DeepResearch vs. Single-Pass RAG across the 11 dimensions:
  - Search Queries
  - Source Diversity
  - Documents Analyzed
  - Gap Detection
  - Topic Organization
  - LLM API Calls (report phase)
  - Report Length
  - Citations
  - Visual Elements
  - Anti-Hallucination Measures
  - Total Runtime
- **Data source:** Metrics derived from the conversation’s evaluation slide content (e.g., 12.4 vs 23.7 sources, ~2,500 vs ~6,800 words, 14.2 vs 5.8 LLM calls, etc.).

### 2. Mention Mermaid Diagram Generation in Synthesis
- **Location:** Section III-E (Phase 5: Hierarchical Synthesis), in the Report Generation paragraph.
- **Action:** Add 1–2 sentences stating that the `ReportBuilder` injects structured visual summaries (e.g., Mermaid flowcharts for causal chains and decision trees) into the markdown report to improve readability.

### 3. Add Report Post-Processing Subsection
- **Location:** Section IV (Implementation Details), after the Progress Callback subsection or within the Synthesis details.
- **Action:** Add a brief paragraph (and possibly a small inline listing) describing the Mermaid post-processor that detects unquoted special characters (`$`, `()`, `+`, etc.) in generated Mermaid blocks and auto-quotes them to guarantee valid syntax.

## Files to Modify
- `D:\prac_prog\fun\tmp\deepsearch3-prev\deep_search\DeepResearch_IEEE_Report.tex`

## Validation
- Ensure the `.tex` file compiles (no malformed table environments, no missing backslashes).
- Verify that new table numbering follows sequentially from Table 2.

## Open Question for User
Should the evaluation matrix use the exact numeric values from the single representative query discussed in Section V-D (e.g., `max_docs=10`, ~127s runtime), or do you want averaged values from multiple queries to be inserted as placeholders?
